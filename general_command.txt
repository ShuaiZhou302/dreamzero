
# infer example

srun -p acd_u -c 8 --gres=gpu:3 --mem=256G --job-name=shuai_dz --pty bash

cd /data/user/wsong890/shuaizhou/dreamzero
conda activate dz_shuai

export LD_LIBRARY_PATH=$CONDA_PREFIX/lib/python3.11/site-packages/torch/lib:$CONDA_PREFIX/lib/python3.11/site-packages/nvidia/cuda_runtime/lib:$CONDA_PREFIX/lib/python3.11/site-packages/nvidia/cudnn/lib:$LD_LIBRARY_PATH

export HF_HOME=/data/user/wsong890/shuaizhou/dreamzero/checkpoints/hf_cache
export HF_HUB_CACHE=$HF_HOME/hub
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1

python - <<'PY'
import pathlib, re
p = pathlib.Path("/data/user/wsong890/shuaizhou/dreamzero/checkpoints/DreamZero-DROID/experiment_cfg/conf.yaml")
s = p.read_text()
s = re.sub(
    r"tokenizer_path:\s*.*",
    "tokenizer_path: /data/user/wsong890/shuaizhou/dreamzero/checkpoints/umt5-xxl",
    s,
)
p.write_text(s)
PY

CUDA_VISIBLE_DEVICES=0,1 python -m torch.distributed.run --standalone --nproc_per_node=2 \
  socket_test_optimized_AR.py \
  --model-path /data/user/wsong890/shuaizhou/dreamzero/checkpoints/DreamZero-DROID \
  --port 8766 \
  --enable-dit-cache



cd /data/user/wsong890/shuaizhou/dreamzero
conda activate dz_shuai

CUDA_VISIBLE_DEVICES=2 python test_client_AR.py --host localhost --port 8766


save log.txt for debug in terminal  
tmux capture-pane -p -S -50000 > ~/dreamzero_infer_log.txt


# posttraining

srun -p acd_u -c 8 --gres=gpu:8 --mem=1024G --job-name=shuai_dz_posttraining --pty bash

cd /data/user/wsong890/shuaizhou/dreamzero
conda activate dz_shuai

# 不写环境变量 = 用脚本里默认的 AGX_ALOHA_DATA_ROOT / OUTPUT_DIR / NUM_GPUS（见 agx_aloha_training.sh）
# 实际跑 cobot 数据 + 自定义输出目录 + 8 卡，用下面这一条（和「只 bash 脚本」是同一条入口，只是覆盖了变量）：
TRAIN_CONSOLE_LOG=1 AGX_ALOHA_DATA_ROOT=/data/user/wsong890/shuaizhou/dreamzero/cobot_data/cobot_dataset OUTPUT_DIR=./checkpoints/agx_aloha_h100_run1 NUM_GPUS=8 bash scripts/train/agx_aloha_training.sh


# infer server example for our model finetuning 

srun -p acd_u -c 8 --gres=gpu:2 --mem=256G --job-name=shuai_dz --pty bash

cd /data/user/wsong890/shuaizhou/dreamzero
conda activate dz_shuai

export LD_LIBRARY_PATH=$CONDA_PREFIX/lib/python3.11/site-packages/torch/lib:$CONDA_PREFIX/lib/python3.11/site-packages/nvidia/cuda_runtime/lib:$CONDA_PREFIX/lib/python3.11/site-packages/nvidia/cudnn/lib:$LD_LIBRARY_PATH

export HF_HOME=/data/user/wsong890/shuaizhou/dreamzero/checkpoints/hf_cache
export HF_HUB_CACHE=$HF_HOME/hub
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1


CUDA_VISIBLE_DEVICES=0,1 python -m torch.distributed.run --standalone --nproc_per_node=2 \
  socket_test_optimized_AR_aloha.py \
  --model-path checkpoints/agx_aloha_h100_run1/checkpoint-50000 \
  --port 8766 \
  --enable-dit-cache